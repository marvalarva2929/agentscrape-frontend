export interface School {
  id: string
  name: string
  rootDomain?: string
  canonicalUrl?: string
  location?: string
  peopleCount?: number
  lastUpdated?: string
  directoryUrl?: string
  directorySearchAvailable?: boolean
}
